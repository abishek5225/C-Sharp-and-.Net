// Write a program to define a class Circle with its data members pi and r and members
// functions getdata() for initializing data members and calculate() for finding area of ac
// circle. Return result from calculate() and display result in main() function. Use pi as
// constant.


/*class Circle
{
    // Data members
    private const double pi = 3.14159;  // pi as a constant
    private double r;                   // radius

    // Member function to initialize data members
    public void GetData()
    {
        Console.Write("Enter the radius of the circle: ");
        r = Convert.ToDouble(Console.ReadLine());
    }

    // Member function to calculate the area of the circle
    public double Calculate()
    {
        return pi * r * r;  // Area = π * r^2
    }
}

class Program
{
    static void Main(string[] args)
    {
        // Create an object of Circle class
        Circle myCircle = new Circle();

        // Initialize data members
        myCircle.GetData();

        // Calculate area
        double area = myCircle.Calculate();

        // Display result
        Console.WriteLine($"The area of the circle is: {area}");
    }
}
*/