/*
Write a program to define a class named Box which has data length, breadth and height
and public functions ReadData() for reading data members and Volume() to calculate
volume of box.
*/

/*
using System;

class Box
{
    // Data members
    private double length;
    private double breadth;
    private double height;

    // Method to read data
    public void ReadData()
    {
        Console.Write("Enter the length of the box: ");
        length = Convert.ToDouble(Console.ReadLine());

        Console.Write("Enter the breadth of the box: ");
        breadth = Convert.ToDouble(Console.ReadLine());

        Console.Write("Enter the height of the box: ");
        height = Convert.ToDouble(Console.ReadLine());
    }

    // Method to calculate the volume
    public double Volume()
    {
        return length * breadth * height;
    }
}

class Program
{
    static void Main()
    {
        // Create an instance of the Box class
        Box myBox = new Box();

        // Call the ReadData method to input the dimensions
        myBox.ReadData();

        // Calculate the volume using the Volume method
        double volume = myBox.Volume();

        // Display the volume
        Console.WriteLine($"The volume of the box is: {volume}");
    }
}
*/