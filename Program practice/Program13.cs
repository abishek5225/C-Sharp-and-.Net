// Modify above program to read length and breadth of a rectangle in main() function and
// supply them in parameterized constructor to initialize its data members.


/*
    // Define the Rectangle class
    class Rectangle
    {
        // Data members
        private int length;
        private int breadth;

        // Parameterized constructor to initialize length and breadth
        public Rectangle(int length, int breadth)
        {
            this.length = length;
            this.breadth = breadth;
        }

        // Method to calculate the area of the rectangle
        public int CalculateArea()
        {
            return length * breadth;
        }
    }

    // Main program class
    class Program13
    {
        static void Main(string[] args)
        {
            // Read length and breadth from the user
            Console.Write("Enter the length of the rectangle: ");
            int length = Convert.ToInt32(Console.ReadLine());

            Console.Write("Enter the breadth of the rectangle: ");
            int breadth = Convert.ToInt32(Console.ReadLine());

            // Create an object of the Rectangle class with user-provided values
            Rectangle rectangle = new Rectangle(length, breadth);

            // Calculate and print the area
            int area = rectangle.CalculateArea();
            Console.WriteLine("The area of the rectangle is: " + area);
        }
    }
*/