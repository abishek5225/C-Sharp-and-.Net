

/*
using System;
using System.Collections.Generic;
using System.Linq;

class Program
{
    static void Main()
    {
        // creating list
        List<int> numbers = new List<int> { 45, 82, 97, 63, 101, 78, 88, 120 };

        // Using LINQ 
        var selectedNumbers = numbers.Where(num => num > 80).OrderByDescending(num => num);

        Console.WriteLine("Numbers greater than 80 in descending order:");
        foreach (var number in selectedNumbers)
        {
            Console.WriteLine(number);
        }
    }
}
*/